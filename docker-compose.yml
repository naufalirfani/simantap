services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: simantap-app:latest
    ports:
      - "5174:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
